services:
  - type: web
    name: ta-fdrl-irf-dashboard
    runtime: python
    plan: free

    buildCommand: pip install -r requirements.txt

    startCommand: python -m streamlit run dashboard.app --server.address 0.0.0.0 --server.port $PORT

    healthCheckPath: /_stcore/health